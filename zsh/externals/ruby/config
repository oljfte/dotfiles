# Environment variables
export PATH="$BREW_PREFIX/opt/ruby/bin:$PATH"
export PATH="$(gem env | grep 'EXECUTABLE DIRECTORY' | awk '{print $4}'):$PATH"

# Functions
function rbenv() {
  unfunction $0
  eval $(rbenv init -)
  $0 $@
}
