name: CI
on:
  push:
      branches:
      - main
  workflow_dispatch:
jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - name: Test
        run: zsh -c "$(curl -fsSL dotfiles.oljfte.com)" && pwd && ls -al && zsh ./dotfiles/test/test.sh
        shell: /bin/zsh {0}
